Migrate MySQL to SQLite:

- use `mysqldump` to create an `sql` file 
- use shell script to converte file to `SQLite:
    https://github.com/mysql2sqlite/mysql2sqlite
- perform a `SQLite` upload to load `sql` file into `SQLite` db?
- Run some `SQL` statements to test if it worked?